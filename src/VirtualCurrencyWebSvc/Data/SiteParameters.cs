namespace VirtualCurrencyWebSvc.Data
{
    // Keep these parameters in sync with all the web pages used to send values to the server
    public enum SiteParameters
    {
        USER_NAME,
        PASSWORD,
        EMAIL,
        LOGIN_TOKEN,
        INFODATA,
        OPERATION,
        FILE_ID,
        FILE_TITLE,
        VIEW_TYPE,
        DESTINATION_FILE_NAME,
        SYNC_KEY,
        USER_ID,
        PERMISSION,
        PERMISSION_ID,
        ROLE_ID,
        USER_EMAIL,
        ROLE_NAME,
        ROLE_DESCRIPTION,
        IMAGE_WIDTH,
        IMAGE_HEIGHT,
        IMAGE_CONTENTS,
        PAYMENT_TYPE,
        PRODUCT_ID,
        APP_KEY,
        NEW_PASSWORD,
    }
}
